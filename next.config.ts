import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Removed output: 'export' for Vercel deployment
  // This enables server-side features like dynamic routes
};

export default nextConfig;
